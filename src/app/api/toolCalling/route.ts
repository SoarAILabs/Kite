const tools = {
    "createRepository": {
        "description": "Create a new repository",
        "parameters": {
            "name": "string",
            "description": "string"
        }
    }
}
